function createTrackList(playlists) {
    /*
    tracks: [{
        playlist: id of playlist
        tracks: [
            ...
        ] 
    }, ...]
     */
    let trackStatistic = [];
    playlists.map((playlist) => {
        
    })
}